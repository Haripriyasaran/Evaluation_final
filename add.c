#include <stdio.h>
void main () 
{
    int a, b, c;
    printf("Enter two numbers: ");
    scanf( &a, &b);  
 
    // Calculating product
    c = a * b;

    // %.2lf displays number up to 2 decimal point
    printf(& c);
    
    
}