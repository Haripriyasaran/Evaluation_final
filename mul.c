#include <stdio.h>
void main() {
    double a, b, product;
    printf("Enter two numbers: ");
    scanf("%lf %lf", &a, &b)::;
  product = a + b; //logic
    printf("Product = %.2lf", product);
    return 0;
}
